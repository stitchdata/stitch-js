{
  "name": "stitch-js-client",
  "version": "0.0.5",
  "description": "",
  "main": "src/index.js",
  "directories": {
    "example": "example"
  },
  "scripts": {
    "build": "browserify src/index.js --standalone Stitch -o dist/stitch-client.js -t [ envify purge ] -t [ babelify --presets [ es2015 ] ]",
    "minify": "uglifyjs --compress --mangle -o dist/stitch-client.min.js dist/stitch-client.js",
    "example": "http-server"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1",
    "babelify": "^7.3.0",
    "browserify": "^14.3.0",
    "envify": "^4.0.0",
    "http-server": "^0.10.0",
    "uglify-js": "^3.0.11"
  }
}
